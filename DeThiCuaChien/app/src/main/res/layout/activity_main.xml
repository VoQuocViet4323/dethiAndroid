<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">


    <LinearLayout
        android:layout_width="409dp"
        android:layout_height="729dp"
        android:orientation="vertical"
        tools:layout_editor_absoluteX="1dp"
        tools:layout_editor_absoluteY="1dp">

        <TableLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <TableRow android:gravity="center">
                <TextView android:text="THONG TIN SINH VIEN"
                    android:textSize="25sp"/>
            </TableRow>
            <TableLayout>
                <TableRow>
                    <TextView android:text="Ma sv"
                        />
                    <EditText android:inputType="text" android:id="@+id/masv" android:hint="Nhap ma sv" android:layout_width="300dp" android:layout_marginLeft="20sp" />
                </TableRow>
                <TableRow>
                    <TextView android:text="Ma lop"
                        />
                    <EditText android:inputType="textPassword" android:id="@+id/malop" android:hint="Nhap ma lop" android:layout_width="300dp" android:layout_marginLeft="20sp" />
                </TableRow>
            </TableLayout>
            <TableLayout>
                <TableRow android:gravity="center">
                    <Button android:id="@+id/btnLogin" android:text="Login" android:layout_marginRight="10sp"/>
                    <Button android:id="@+id/btnReset" android:text="Reset"/>
                </TableRow>
            </TableLayout>
        </TableLayout>
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>